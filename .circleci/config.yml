version: 2
jobs:
  build:
    docker:
      - image: debian:stretch
    steps:
      - checkout
      - run:
          name: Compile
          command: make
      - run:
          name: Say hi
          command: hello
